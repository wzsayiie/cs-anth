#!/bin/sh

set -e -u

mkdir -p bin
mkdir -p lib

_out="lib/libh"
_inc="-Ialibh"
_src="alibh/*.c"

if [ `uname -s` = "Darwin" ]; then
    cc -dynamiclib   -o $_out.dylib $_inc $_src
else
    cc -fPIC -shared -o $_out.so    $_inc $_src
fi
